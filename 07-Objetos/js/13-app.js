
const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}

// Se utiliza para saber las llaves
console.log(Object.keys(producto));

// Se utiliza para saber los valores
console.log(Object.values(producto));

// Se utliza para saber todos en general
console.log(Object.entries(producto));
