const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}

console.log(producto);

console.log(producto.nombre);
console.log(producto.precio);
console.log(producto.disponible);

// Esta es otra manera de acceder a la llave del objeto.
console.log(producto["Nombre"]);
// no es muy comun esta pero es importante conocer esta version